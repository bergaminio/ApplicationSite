# Ohne MODE=PostgreSQL: in dem Modus kennt H2 den Typ BLOB nicht, und
# an den Notenausweisen haengt genau der. Die Abfragen hier sind
# einfach genug, dass der Unterschied sonst nicht auffaellt.
spring.datasource.url=jdbc:h2:mem:test;DB_CLOSE_DELAY=-1
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=false

app.jwt.secret=nur-fuer-tests-mindestens-zweiunddreissig-zeichen-lang
app.admin.username=testadmin
app.admin.password=testadmin123

app.cors.allowed-origins=http://localhost:5173
app.contact.place=Teststrasse 1
app.contact.phone=+41 00 000 00 00
app.cv.file=
